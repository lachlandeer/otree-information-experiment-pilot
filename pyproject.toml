[project]
name = "otree-information-experiment-pilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lachlan Deer", email = "lachlan.deer@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "black==21.11b1",
    "otree==5.11.5",
    "psycopg2>=2.8.4",
    "sentry-sdk>=0.7.9",
]

[project.scripts]
otree-information-experiment-pilot = "otree_information_experiment_pilot:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
